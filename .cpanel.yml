---
deployment:
  tasks:
    - export DEPLOYPATH=/home/erraezuq/errand.errandr.shop/
    - /bin/cp -R .output/public/* $DEPLOYPATH/
    - /bin/cp .htaccess $DEPLOYPATH/ 2>/dev/null || true
    - /bin/cp .env $DEPLOYPATH/ 2>/dev/null || true